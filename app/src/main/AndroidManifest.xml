<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deepi.fiesta">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/fiesta"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main" />
        <activity android:name=".Login"
            android:label="Login"/>
        <activity android:name=".HomeScreen"
            android:label="Home"/>
        <activity android:name=".AllPlaces"
            android:label="Places"/>
        <activity android:name=".AllVenues"
            android:label="Venues"/>
        <activity android:name=".Admin"
            android:label="Admin"/>
        <activity android:name=".ShowVenue"
            android:label="Venues"/>
        <activity android:name=".ShowDescription"
            android:label="Description"/>
        <!--
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        -->
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Book"
            android:label="Book"/>
        <activity android:name=".Signup"
            android:label="Signup"/>
        <activity android:name=".ViewPhotoActivity"
            android:label="Gallery"/>
        <activity android:name=".PlanEvents"
            android:label="Plan Events"/>
        <activity android:name=".Wedding"
            android:label="Wedding"/>
        <activity android:name=".CustomizeWedding"
            android:label="Customize Wedding"/>
        <activity android:name=".Birthday"
            android:label="Birthday"/>
        <activity android:name=".CustomizeBirthday"
            android:label="Customize Birthday"/>
        <activity
            android:name=".WeddingPackages"
            android:label="@string/title_activity_wedding_packages"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BirthdayPackages"
            android:label="@string/title_activity_birthday_packages"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PickYourVenue"
            android:label="Pick Your Venue"/>
        <activity android:name=".DisplayBooking"
            android:label="Display Booking"/>
    </application>

</manifest>